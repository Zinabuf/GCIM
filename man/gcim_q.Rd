% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gcim_quantitative.R
\name{gcim_q}
\alias{gcim_q}
\title{Perform regression analysis for genetic causality inference model(GCIM) with quantitative outcome}
\usage{
gcim_q(
  qp_tar_phen,
  qp_tar_cov,
  Add_PRS,
  Int_PRS,
  Cov_PRS,
  verbose = TRUE,
  scale_prs = TRUE,
  save_temp_files = TRUE
)
}
\arguments{
\item{qp_tar_phen}{File path for the target phenotype data (FID, IID, Outcome format)}

\item{qp_tar_cov}{File path for the target covariate data (FID, IID, Covariate, Confounders format)}

\item{Add_PRS}{Either file path or data frame for additive PRS values (fallback if files not found)}

\item{Int_PRS}{Either file path or data frame for interaction PRS values (fallback if files not found)}

\item{Cov_PRS}{Either file path or data frame for covariate PRS values (fallback if files not found)}

\item{verbose}{Logical, whether to print progress messages (default: TRUE)}

\item{scale_prs}{Logical, whether to scale PRS values (default: TRUE)}

\item{save_temp_files}{Logical, whether to save temporary PRS files (default: TRUE)}
}
\value{
List containing model summary and diagnostic information
}
\description{
This function performs linear regression analysis for GCIM
with quantitative outcomes using Polygenic Risk Scores (PRS).
It reads PRS files that were previously saved by PRS_quantitative function.
}
\examples{
\dontrun{
# After running PRS_quantitative functions to generate PRS files:
# add_prs <- PRS_quantitative(plink_path, "DummyData", summary_input = add)
# int_prs <- PRS_quantitative(plink_path, "DummyData", summary_input = gxe)  
# cov_prs <- PRS_quantitative(plink_path, "DummyData", summary_input = trd)

result <- gcim_q("Qphe_target.txt", "Qexp_target.txt", 
                 Add_PRS = add_prs, Int_PRS = int_prs, Cov_PRS = cov_prs)
}
}
