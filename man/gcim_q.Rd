% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/GCIM_quantitative.R
\name{gcim_q}
\alias{gcim_q}
\title{Perform regression analysis for genetic causality inference model(GCIM) with quantitative outcome}
\usage{
gcim_q(
  qp_tar_phen,
  qp_tar_cov,
  Add_PRS = NULL,
  Int_PRS = NULL,
  Cov_PRS = NULL,
  temp_dir = tempdir(),
  verbose = TRUE,
  scale_prs = TRUE,
  save_temp_files = TRUE
)
}
\arguments{
\item{qp_tar_phen}{File path for the target phenotype data (FID, IID, Outcome format)}

\item{qp_tar_cov}{File path for the target covariate data (FID, IID, Covariate, Confounders format)}

\item{Add_PRS}{data frame for additive PRS values or NULL to read from temp files}

\item{Int_PRS}{data frame for interaction PRS values or NULL to read from temp files}

\item{Cov_PRS}{data frame for covariate PRS values or NULL to read from temp files}

\item{temp_dir}{Directory path containing PRS files (default: tempdir())}

\item{verbose}{Logical, whether to print progress messages (default: TRUE)}

\item{scale_prs}{Logical, whether to scale PRS values (default: TRUE)}

\item{save_temp_files}{Logical, whether to save temporary PRS files (default: TRUE)}
}
\value{
List containing model summary and diagnostic information
}
\description{
This function performs linear regression analysis for GCIM
with quantitative outcomes using Polygenic Risk Scores (PRS).
It can read PRS files from temporary directories or use provided PRS data objects.
}
\examples{
\dontrun{
# Method 1: Using PRS_quantitative outputs directly
q <- PRS_quantitative(plink_path, "DummyData", summary_input = add)  # Additive PRS
r <- PRS_quantitative(plink_path, "DummyData", summary_input = gxe)  # Interaction PRS  
p <- PRS_quantitative(plink_path, "DummyData", summary_input = trd)  # Covariate PRS

result <- gcim_q("Qphe_target.txt", "Qexp_target.txt", 
                 Add_PRS = q, Int_PRS = r, Cov_PRS = p)

# Method 2: Reading from .prs.sscore files in temp directory
result <- gcim_q("Qphe_target.txt", "Qexp_target.txt", 
                 Add_PRS = NULL, Int_PRS = NULL, Cov_PRS = NULL,
                 temp_dir = "/path/to/prs/files")
}
}
